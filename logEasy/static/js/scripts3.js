const salvar_bt = document.getElementById('salvar_bt')
const botao_voltar = document.getElementById('botao_link')

salvar_bt.addEventListener('click', () => {
    window.alert('Edição concluída!')
});

botao_voltar.addEventListener('click', () => {
    window.alert('Edição Cancelada!')
});
